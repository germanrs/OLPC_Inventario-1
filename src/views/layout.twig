<!DOCTYPE html>
<html lang="en">
	<head>
		<title>Inventario</title>
		<meta charset="UTF-8" />
		<meta http-equiv="cleartype" content="on" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
		{# Global Css Start #}
		<link rel="stylesheet" media="screen" href="{{ app.request.baseUrl }}/css/globalStyle.css" />
		
		{# Global Css END #}
		{% block pageCss %}
		{% endblock %}

	</head>
	<body>
		<header>
			<a href="{{ app.request.baseUrl }}" id="logo" ></a>
			<div class="headdercontent"></div>
			{% if access_level > 299 %}
				<ul class="main-menu">
					<li class="{% if 'Inventory' in app.request.attributes.get('_route') %}active{% endif %}"> <a href="{{ path('Inventory.laptops') }}">Inventario</a></li>
					<li class="{% if 'Import' in app.request.attributes.get('_route') %}active{% endif %}"> <a href="{{ path('Import') }}">Importar</a></li>				
					<li class="{% if 'Export' in app.request.attributes.get('_route') %}active{% endif %}"> <a href="{{ path('Export') }}">Exportar</a></li>
					{% if access_level > 499 %}
					<li class="{% if 'Admin' in app.request.attributes.get('_route') %}active{% endif %}"> <a href="{{ path('Admin') }}">Administrar</a></li>
					{% endif %}
				</ul>
			{% endif %}
				<div class="side-menu">
					<div id="holaUsername">Hola {{username}}</div>
					<a class="auth-link" href="{{ app.request.baseUrl }}/Auth/logout">Cerrar sesión</a>
				</div>
		</header>
		<div class="clear"></div>
		<div class="wrapper">
			<div class="page-content clearfix">
				{# pageContent Start #}
				{% block pageContent %}
				{% endblock %}
				{# pageContent END #}
			</div>
		</div>
		<div class="push"></div>
		<div class="clear"></div>
		<footer>
			© 2016 Fundación Zamora Terán. Todos los derechos reservados.
		</footer>

		<script src="{{ app.request.baseUrl }}/js/jquery-2.1.3.min.js"></script>
		{# pageScripts Start #}
		{% block pageScripts %}
		{# pageScripts END #}
		{% endblock %}
	</body>
</html>